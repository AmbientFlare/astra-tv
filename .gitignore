*.iml
*.swp
.idea
.DS_Store
.vscode
.tmp

dist
build
reference
logs
tmp
START_HERE.md

# Local engineering, release-operations, and submission notes. These files are
# intentionally retained in the working copy but are not public documentation.
docs/amazon-submission-*.md
docs/release-build.md
docs/store-listing.md

# Throwaway audio playback spike (2026-07-27). Contains hardcoded credentials
# by design. Delete the directory and revert src/App.tsx when finished.
src/spike
buildinfo.json
node_modules
CMakeUserPresets.json
npm-debug.log

# agent handoffs / working notes (never publish)
docs/handoffs/

# Closed-caption investigation notes. Held back deliberately: the current
# revision names local infrastructure (GPU VM, Jellyfin container, hardware).
# Publish a sanitised revision once the caption work is settled, then drop
# this entry.
docs/CLOSED_CAPTION_NOTES.md

*.iml
*.swp
.idea
.DS_Store
.vscode
.tmp

dist
build
reference
logs
tmp

# Throwaway audio playback spike (2026-07-27). Contains hardcoded credentials
# by design. Delete the directory and revert src/App.tsx when finished.
src/spike
buildinfo.json
node_modules
CMakeUserPresets.json
npm-debug.log

# Closed-caption investigation notes. Held back deliberately: the current
# revision names local infrastructure. Publish a sanitised revision once the
# caption work is settled, then drop this entry.
docs/CLOSED_CAPTION_NOTES.md

# Device test notes: local working log, same reason.
docs/DEVICE_TEST_NOTES.md

# agent handoffs / working notes (never publish)
docs/handoffs/
